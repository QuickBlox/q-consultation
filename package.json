{
  "name": "q-consultation-lite",
  "version": "1.2.4-plus",
  "license": "MIT",
  "author": {
    "name": "Injoit LTD",
    "email": "sales@quickblox.com"
  },
  "scripts": {
    "dev": "lerna run dev",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "start:api": "lerna run --scope q-consultation-api start",
    "start:pages": "lerna run --scope integration-pages demo",
    "init:config": "node ./bin/initConfig.js",
    "init:schema": "node ./bin/initSchema.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "dotenv": "^16.0.3",
    "js-yaml": "^4.1.0",
    "lerna": "^6.0.1",
    "prettier": "^2.4.0",
    "readline-sync": "^1.4.10"
  }
}
