{
  "name": "q-consultation-lite",
  "version": "2.0.0-alpha.4",
  "license": "MIT",
  "author": {
    "name": "Injoit LTD",
    "email": "sales@quickblox.com"
  },
  "scripts": {
    "dev": "lerna run dev",
    "build": "lerna run --ignore q-consultation-doc build",
    "start:api": "lerna run --scope q-consultation-api start",
    "start:doc": "lerna run --scope q-consultation-doc start",
    "start:pages": "lerna run --scope integration-pages start",
    "lint": "lerna run lint",
    "init:config": "node ./bin/initConfig.js",
    "init:schema": "node ./bin/initSchema.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "dotenv": "^16.0.3",
    "js-yaml": "^4.1.0",
    "lerna": "^6.0.1",
    "prettier": "^2.4.0",
    "readline-sync": "^1.4.10"
  }
}
